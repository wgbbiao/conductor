status: NEEDS_CONTEXT
commits: feat(api): artifacts service + GET /tool-runs/:id/diff
test summary: `pnpm --filter @conductor/api lint` failed and `pnpm --filter @conductor/api build` failed on existing out-of-scope TypeScript errors in `apps/api/src/tools/codex-tool-provider.ts`
concerns: Task 7 changes are implemented, but required verification is blocked until `apps/api/src/tools/codex-tool-provider.ts` is fixed by its owner or scope is expanded
